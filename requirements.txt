# Minimal install. Prefer editable extras from pyproject.toml:
#   pip install -e .
#   pip install -e ".[dev]"
#   pip install -e ".[api]"
#   pip install -e ".[hardware]"
#   pip install -e ".[train]"
pyyaml>=6.0
